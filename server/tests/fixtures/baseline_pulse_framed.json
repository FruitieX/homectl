{
  "id": "baseline_pulse_framed",
  "description": "P02 E01: two queued reports are evaluated against their own coherent frame, so the first (true) pulse fires and the second (false) does not.",
  "baseline_revision": "5aef129c",
  "groups": {},
  "routines": [
    {
      "id": "pulse",
      "name": "Pulse",
      "rules": [
        {
          "state": { "value": true },
          "trigger_mode": "pulse",
          "integration_id": "test",
          "device_id": "button"
        }
      ],
      "actions": [
        { "action": "ForceTriggerRoutine", "routine_id": "noop" }
      ]
    }
  ],
  "steps": [
    {
      "step": "queue_report",
      "device": {
        "id": "button",
        "name": "Button",
        "integration_id": "test",
        "data": { "Sensor": { "value": true } },
        "raw": null
      }
    },
    {
      "step": "queue_report",
      "device": {
        "id": "button",
        "name": "Button",
        "integration_id": "test",
        "data": { "Sensor": { "value": false } },
        "raw": null
      }
    },
    { "step": "flush" }
  ]
}
